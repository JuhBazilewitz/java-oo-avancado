package br.com.praticasoo.exercicio;

/**
 * 
 * @author juliane.bazilewitz
 *
 */
public class Exercicio {

    /*
    criar uma classe de persitencia chamada cliente (nome e rg) #
    criar um classe de persistencia chamada conta (data de abertura e valor) #
    criar uma classe de repositorio generico #
    no repositorio deve haver um atributo de lista T #
    no repositorio deve haver um metodo para adicionar item na lista T #
    ao adicionar deve ser verificado se o item ja esta na lista T #
    no repositorio deve haver um metodo para remover um item na lista T #
    ao remover será informado o codigo identificador da entidade para remoção
    no repositorio deve haver um metodo para salvar todos os itens contidos na lista T
    o salvar de fato deve estar nas entidades cliente e conta
    ao salvar pelo repositorio a lista T deve ser limpa
    
    sugestoes:
        ter uma classe abstrata que define o identificador e um metodo abstrato para salvar
        ou
        ter uma interface que define o get identificador e um metodo para salvar
    */
}
